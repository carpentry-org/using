(load "git@github.com:hellerve/file.git@master")
(load "main.carp")

(defmodule Int
  (sig close (Fn [Int] ()))
  (defn close [i]
    ()))

(defn main []
  (using-do (File.open "example") f
    (File.write &f "hi")
    (File.rewind &f)
    (IO.println &(File.read &f 2))))
